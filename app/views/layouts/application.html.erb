<!DOCTYPE html>
<!--[if IE 8]> <html lang="en" class="ie8" ng-app> <![endif]-->
<!--[if IE 9]> <html lang="en" class="ie9" ng-app> <![endif]-->
<!--[if !IE]><!-->
<html lang="en" ng-app="tableforeight">
<!--<![endif]-->
    <head>
        <title>Table For Eight</title>
        
      <!-- Meta -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Table for Eight">
        <meta name="keywords" content="restraunt, organize, socialize">
        <%= favicon_link_tag "logo-icon.png" %>
        <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
        <%= csrf_meta_tags %>
    </head>
    <body ng-controller="eventCtrl">
        <div>

            <!-- Header -->
            <header class="mainheader">
                <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
                    <div class="container">
                        <!-- Brand and toggle get grouped for better mobile display -->
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-datanavbar-collapsePanel">
                                <span class="sr-only">Toggle navigation</span>
                            </button>
                            <a class="navbar-brand" href="#">
                                <%= image_tag("Table-For-Eight-logo.png", :class => "logo") %>
                            </a>
                        </div>
                
                        <!-- Collect the nav links, forms, and other content for toggling -->
                        <div class="collapse navbar-collapse" id="bs-datanavbar-collapsePanel">
                            <ul class="nav navbar-nav">
                                <li><a href="#what" class="scrollto">What?</a></li>
                                <li><a href="#who" class="scrollto">Who?</a></li>
                                <li><a href="#where" class="scrollto">Where?</a></li>
                                <li><a href="#when" class="scrollto">When?</a></li>
                                <li><a href="#go" class="scrollto">Go!</a></li>
                            </ul>
                        </div><!-- /.navbar-collapse -->
                    </div><!-- /.container-fluid -->
                </nav>
            </header>
            <!--/header-->

            <%= yield %>

            <footer>
                <p>Table For Eight &copy; Copyright 2014.</p>
            </footer>
        </div>

        <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
        <script type="text/javascript">
            $('.form_datetime').datetimepicker({
                //language:  'fr',
                weekStart: 1,
                todayBtn:  1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 2,
                forceParse: 0,
                showMeridian: 1
            });
            $('.form_date').datetimepicker({
                language:  'fr',
                weekStart: 1,
                todayBtn:  1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 2,
                minView: 2,
                forceParse: 0
            });
            $('.form_time').datetimepicker({
                language:  'fr',
                weekStart: 1,
                todayBtn:  1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 1,
                minView: 0,
                maxView: 1,
                forceParse: 0
            });
            $('#slider').cycle({
                fx : 'scrollVert',
                timeout: 3000,
                speed: 200,
                slides: '.slide'
            });
            $('.navbar').waypoint('sticky');
            $('.navbar a, a.scrollto, a#arrow_down').click(function(e){
                $('html,body').scrollTo(this.hash, this.hash, {'axis':'y'});
                e.preventDefault();
            });
            $(function() {
                Grid.init();
            });
        </script>

    </body>
</html>
